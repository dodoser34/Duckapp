# Direct runtime dependencies only; pip resolves the transitive ones.
#
# Removed from the previous list because nothing imported them:
#   customtkinter, darkdetect (desktop GUI toolkit, unused)
#   aiosqlite, tqdm, soupsieve (unused)
#   passlib (bcrypt is used directly)
#   python-jose + ecdsa/rsa/pyasn1 (a second JWT stack next to PyJWT)

fastapi==0.116.1
uvicorn==0.35.0
starlette==0.47.3
pydantic==2.13.4

PyMySQL==1.1.2
python-dotenv==1.1.1

PyJWT==2.10.1
bcrypt==4.3.0
email-validator==2.3.0
python-multipart==0.0.12

# imghdr was removed from the stdlib in Python 3.13.
standard-imghdr==3.13.0
